<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- صلاحيات الموقع الجغرافي -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- صلاحيات الإنترنت -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- صلاحيات أخرى مطلوبة -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- صلاحيات OneSignal -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <application
        android:label="vcenter"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBgeRLX_sa19dBqj75ovixwgnr21ZV-xzU" />
        
        <!-- OneSignal App ID -->
        <meta-data
            android:name="onesignal_app_id"
            android:value="806c1a69-cd15-41b1-8f83-d8a8b3f218f6" />
        
        <!-- OneSignal Google Project Number -->
        <meta-data
            android:name="onesignal_google_project_number"
            android:value="414036126974" />
        
        <!-- OneSignal Notification Channel -->
        <meta-data
            android:name="onesignal_notification_channel_name"
            android:value="VCenter Notifications" />
        
        <!-- OneSignal Notification Channel Description -->
        <meta-data
            android:name="onesignal_notification_channel_description"
            android:value="إشعارات تطبيق VCenter" />
        
        <!-- OneSignal Notification Icon -->
        <meta-data
            android:name="onesignal_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        
        <!-- OneSignal Notification Large Icon -->
        <meta-data
            android:name="onesignal_notification_large_icon"
            android:resource="@mipmap/ic_launcher" />
        
        <!-- OneSignal Notification Accent Color -->
        <meta-data
            android:name="onesignal_notification_accent_color"
            android:value="FF9C27B0" />
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
